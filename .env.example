# Copy to .env and fill in. .env is gitignored — never commit real credentials.

# Virtual Framer login (https://backend.virtualframer.com)
VF_USERNAME=
VF_PASSWORD=

# REQUIRED. The Google Calendar to write to. Find it in Google Calendar:
# Settings -> pick the calendar -> Integrate calendar -> Calendar ID.
VF_CALENDAR_ID=
